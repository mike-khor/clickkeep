{
  "name": "@clickkeep/session-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "echo 'worker tests TODO — use @cloudflare/vitest-pool-workers'",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@clickkeep/sync-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
