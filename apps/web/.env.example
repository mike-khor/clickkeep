# URL of the session worker. In dev, default to local wrangler. In prod, point to the
# deployed worker (e.g. https://clickkeep-session.your-subdomain.workers.dev).
VITE_SESSION_WORKER_URL=http://localhost:8787
